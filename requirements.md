## Totebag Flow

It will be just like the frame flow but with some differences.

Main difference is that it will totebag image where the users will put their images and see the preview of the totebag.

# Type

For now it will be static and so we will use dummy data. for tag options we have black and white. the images are from /assets/images/totebag.png and /assets/images/totebag black.png. use dummy data for the other options we have in the ui that are coming from the api.

# Size

For now it will be static and so we will use dummy data. for size options we have 12" x 16" and 16" x 20". the images are from /assets/images/totebag.png and /assets/images/totebag black.png. use dummy data for the other options we have in the ui that are coming from the api.

# Main Difference

the main difference between frame and totebag is that the frame is put around the image but here the image will be put inside the totebag.

# Flow

The flow will be same as the frame flow.

# UI & UX

maintain the same ui and ux as the frame flow.

# API

We will integrate apis later. for now use dummy data.

# Remember

remember:

1. dont put unnecessary comments that look ai generated. put short comments to seperate modules, sections and features and where necessary
2. follow the coding style and the folder structure of the project
3. use the loading spinner component where it is necessary
4. do error handling so that we have a fantastic user experience
5. show toast popups where necessary
6. the user experience should be top notch
7. use the reusable components we have
